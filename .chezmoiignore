.vscode/
# いったんインストールをあきらめる
# setup.ps1

# Windows「ではない」場合、Windows専用のファイル・ディレクトリを除外する
{{ if ne .chezmoi.os "windows" }}
AppData/
setup.ps1
{{ end }}

# Linux「ではない」場合、Linux専用のスクリプトを除外する
{{ if ne .chezmoi.os "linux" }}
setup.sh
{{ end }}